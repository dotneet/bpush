var global = ("global",eval)("this");
global['_bpush_env'] = {
  endpoint_base: "{{ endpoint_base }}",
  app_key: "{{ app_key }}",
  vapid: {% if constant('USE_VAPID_PROTOCOL') %}true{% else %}false{% endif %},
  vapidPublicKey: new Uint8Array([{{ vapid_public_key }}]),
  simple_embedded: {% if simple_embedded %}true{% else %}false{% endif %}
};

